{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && pnpm install --frozen-lockfile && pnpm --filter api prisma generate && pnpm --filter api build"
  },
  "deploy": {
    "startCommand": "pnpm --filter api exec prisma db push --skip-generate && pnpm --filter api start:prod",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
